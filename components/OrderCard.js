import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const OrderCard = () => {
  return (
    <View style={styles.mainContainer}>
      <Text>Order Card Template</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  mainContainer: {},
});

export default OrderCard;
